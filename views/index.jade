!!! html
html
  head
    meta(http-equiv="Content-Type", content="text/html", charset="utf-8")
    title Welcome to Woothee!
  body
    div
      Try to parse your UserAgent!
    form(method="GET", action="/")
      input(type="text", name="a", value=agent, size=200)
      input(type="submit", value="parse!")
    div
      For json data, try
      a(href="/json?a=#{agente}") /json
      With query parameter 'a'
    if parsed
      table
        tr
          th UserAgent
          td= agent
        tr
          th name
          td= parsed['name']
        tr
          th version
          td= parsed['version']
        tr
          th category
          td= parsed['category']
        tr
          th os
          td= parsed['os']
        tr
          th vendor
          td= parsed['vendor']

        
      